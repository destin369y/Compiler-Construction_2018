procedure main {
 int one, zero, temp;
 
 one  = 1;
 zero = 0;

 if (zero or zero) then
     write one;
 else 
     write zero;

 if (one or zero) then
     write one;
 else 
     write zero;

 if (zero or one) then
     write one;
 else 
     write zero;

 if (one or one) then
     write one;
 else 
     write zero;

 if (zero and zero) then
     write one;
 else 
     write zero;

 if (one and zero) then
     write one;
 else 
     write zero;

 if (zero and one) then
     write one;
 else 
     write zero;

 if (one and one) then
     write one;
 else 
     write zero;

 if (not zero) then
     write one;
 else 
     write zero;

 if (not one) then
     write zero;
 else 
     write one;

}